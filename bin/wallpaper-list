#!/bin/bash

find /home/est/.local/share/HybridOS/wallpapers -mindepth 1 -maxdepth 1 \( -iname "*.png" -o -iname "*.jpg" \) | sort | while read -r path; do
  echo "$(basename "$path" | sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g')"
done
